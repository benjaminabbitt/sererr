--- sererr/src/lib.rs
+++ delete field file from struct StackFrame expression in capture_frames
@@ -250,17 +250,17 @@
                 .filename()
                 .map(|p| p.display().to_string())
                 .unwrap_or_default();
             let line = symbol.lineno().unwrap_or(0);
             let in_app = is_app_frame(&function);
 
             out.push(StackFrame {
                 function,
-                file,
+                 /* ~ changed by cargo-mutants ~ */
                 line,
                 in_app,
                 ..Default::default()
             });
         }
     }
     out
 }
