[project]
name = "sererr-conformance"
version = "0.0.0"
description = "Python conformance runner for the shared sererr cucumber corpus."
requires-python = ">=3.10"
dependencies = [
    "behave>=1.2.6",
    "protobuf>=4.25,<7",
    "sererr",
]

[tool.uv.sources]
sererr = { path = "../../../../packages/python", editable = true }
